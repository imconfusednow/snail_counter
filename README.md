# Snail Counter

Discord bot which counts how many snail reacts each user gets.

## To start

```
npm install
Create config.json in root dir
npm run register_commands
npm run run_migrations
npm start
```

config.json
```json
{
	"TOKEN": "",
    "CLIENT_ID": "",
    "GUILD_ID": "",
    "STATUS": ""
}
```
